# 依赖清单（已瘦身：移除仓库内 0 引用的重型包，避免 Streamlit Cloud 构建超时/被拒 499/REFUSAL）
# 被移除的包（确认全仓库无任何 .py import）：python-docx, pdfplumber, altair(Streamlit 自带),
#   cloudscraper, fake-useragent, easyocr(拉 torch 极重), pdf2image(需系统 poppler), spacy(拉大模型)
# 若后续某页面真要用到，再加回对应行即可。

streamlit>=1.37.0
pandas>=1.5
numpy>=1.24
openpyxl>=3.0
websocket-client>=1.3
requests>=2.28
beautifulsoup4>=4.11
playwright>=1.40
plotly>=5.18
oss2
prophet
pillow
markitdown
folium>=0.15.0
streamlit-folium>=0.20.0
